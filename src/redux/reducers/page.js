import {} from '../actions';

const defaultState = {
  page: 1,
};

const page = (state = defaultState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default page;

